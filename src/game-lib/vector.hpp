#pragma once
#include "wrap-hwlib.hpp"

namespace gamelib {
    template<typename T, size_t SIZE>
    class Vector {
        static_assert();

    public:
        



    
    private:
        std::array<T, SIZE> _items;

    };
}